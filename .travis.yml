language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t murthydevata/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true murthydevata/docker-react npm run test
